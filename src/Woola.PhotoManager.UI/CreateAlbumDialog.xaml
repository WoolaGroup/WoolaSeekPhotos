<Window x:Class="Woola.PhotoManager.UI.CreateAlbumDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nuevo álbum" Width="360" Height="210"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1A1A1A" FontFamily="Segoe UI" ShowInTaskbar="False">

    <StackPanel Margin="24">
        <TextBlock Text="Nombre del álbum" FontSize="11" Foreground="#888" Margin="0,0,0,4"/>
        <TextBox x:Name="NameBox" FontSize="14" Padding="8,6"
                 Background="#252525" Foreground="White" BorderBrush="#444"
                 CaretBrush="White" MaxLength="80"/>

        <TextBlock Text="Descripción (opcional)" FontSize="11" Foreground="#888" Margin="0,14,0,4"/>
        <TextBox x:Name="DescBox" FontSize="13" Padding="8,6"
                 Background="#252525" Foreground="White" BorderBrush="#444"
                 CaretBrush="White" MaxLength="200"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancelar" Click="Cancel_Click"
                    Background="#2A2A2A" Foreground="#888" BorderThickness="0"
                    Padding="14,8" Cursor="Hand" Margin="0,0,10,0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="Crear" Click="Create_Click" IsDefault="True"
                    Background="#0078D4" Foreground="White" BorderThickness="0"
                    Padding="14,8" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </StackPanel>
</Window>

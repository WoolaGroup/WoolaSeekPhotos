<Window x:Class="Woola.PhotoManager.UI.PhotosByPersonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fotos de la persona" Height="500" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="#1A1A1A">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="PhotosList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#252525" CornerRadius="6" Margin="5" Width="180">
                            <StackPanel>
                                <Image Source="{Binding ThumbnailPath}" Height="120" Stretch="Uniform"/>
                                <TextBlock Text="{Binding FileName}" FontSize="10" Foreground="#CCCCCC" 
                                           TextWrapping="Wrap" Margin="8,5" MaxHeight="30"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>